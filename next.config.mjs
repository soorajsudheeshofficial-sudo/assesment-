/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/landing-page-design",
  assetPrefix: "/landing-page-design/",
  images: {
    unoptimized: true,
  },
}

export default nextConfig
